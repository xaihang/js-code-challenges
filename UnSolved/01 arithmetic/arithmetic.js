// DIRECTION:
// Implement four arithmetic functions that perform addition, 
// subtraction, multiplication, and division operations respectively. 
// These functions should accept two numerical inputs, num1 and num2, 
// and return the result of the operation as a numerical value. 
// The add function should return the sum of num1 and num2, 
// the subtract function should return the difference between num1 and num2, 
// the multiply function should return the product of num1 and num2, 
// and the divide function should return the quotient of num1 and num2. 
// These functions should be exported as part of an object containing all four functions.


const add = function(num1, num2) {
  
};

const subtract = function(num1, num2) {
  
};

const multiply = function(num1, num2) {
  
};

const divide = function(num1, num2) {
  
};


// Please do not modify the code below, make changes only to the code above.
module.exports = {add, subtract, multiply, divide};